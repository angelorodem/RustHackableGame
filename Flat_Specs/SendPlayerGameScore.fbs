include "Player.fbs";
include "GameResult.fbs";

enum Status : byte {
    Ok,
    Fail
}

table SendGameScore {
    player: Player;
    game_result: GameResult;
    score_message: string;
    status: Status;
}

root_type SendGameScore;